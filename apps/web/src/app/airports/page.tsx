import { AirportsPageClient } from "./AirportsPageClient";

export const metadata = {
  title: "Airports — Flight Tracker",
};

export default function AirportsPage() {
  return <AirportsPageClient />;
}
